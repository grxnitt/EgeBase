export type DictionaryFeatureEntry = {
  title: string;
  sourceTitle: string;
  slug: string;
  section: string;
  features: string[];
};

export const dictionaryFeatureCatalog: DictionaryFeatureEntry[];
